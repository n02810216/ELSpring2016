hello.py is flask script, must be run with virtualenv with .html files in root/templates and data files and dependencies in root/templates

about.html is the web page containing all JS code for the chart
